<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: allows plain http to the local dev backend (Android emulator's
     10.0.2.2 alias for the host machine, localhost, plus the dev machine's
     LAN IP for testing on a physical device — keep this in sync with
     ProductService._physicalDeviceBackendHost). Not included in release
     builds, so production traffic still requires https. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">192.168.29.218</domain>
    </domain-config>
</network-security-config>
